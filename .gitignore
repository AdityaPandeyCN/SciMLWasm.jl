Manifest.toml
node_modules
public
.vercel

# The built demos (examples/*/*.wasm and the generated index.html pages) are
# committed on purpose: the site deploys straight from git. See vercel.json.
.env*
